# Clash → v2rayN 转换原理 - 简明版

> 5 分钟快速理解转换工具的核心原理

---

## 🎯 核心问题

**问题**: Clash 配置无法直接导入 v2rayN

**原因**:
1. 格式不同 (YAML vs JSON)
2. 规则类型不同
3. Xray 26.x 对域名有严格的 LDH 规范要求

---

## 🔄 转换流程 (5 步)

```
1. 读取 clash.yml
   ↓
2. 按策略分组 (9,816 条 → 12 组)
   ↓
3. 转换规则类型
   - DOMAIN → full:xxx
   - DOMAIN-SUFFIX → domain:xxx
   - DOMAIN-KEYWORD → 删除 ❌
   - IP-CIDR → 独立数组元素
   ↓
4. 验证域名 (LDH 规范)
   - 必须含 .
   - 不能以 - 开头或结尾
   - 只能含字母/数字/连字符
   ↓
5. 输出 JSON 文件
```

---

## 🔑 三个关键点

### 1. IP 规则必须独立

**❌ 错误**:
```json
{"ip": ["8.8.8.8/32\n1.1.1.1/32"]}
```

**✅ 正确**:
```json
{"ip": ["8.8.8.8/32", "1.1.1.1/32"]}
```

**原因**: v2rayN 的 `ip` 字段不支持 `\n` 拼接

---

### 2. 域名必须符合 LDH 规范

**LDH 规范**: 每个 label 只能包含字母、数字、连字符,且不能以连字符开头或结尾

**❌ 非法域名**:
- `domain:cn` (不含 .)
- `domain:fc-.cdn.com` (label 以 - 结尾)
- `domain:.example.com` (空 label)

**✅ 合法域名**:
- `domain:google.com`
- `domain:cdn-example.com`
- `full:api.example.com`

---

### 3. DOMAIN-KEYWORD 必须删除

**原因**: Xray 26.x 对 keyword 也执行 LDH 校验,无法绕过

**影响**: 约 100 条规则 (1.6%)

**补救**: 手动添加具体域名

---

## 📊 转换结果

| 项目 | 数值 |
|-----|------|
| 原始规则 | 9,816 条 |
| 生成路由组 | 12 个 |
| 保留规则 | 9,651 条 |
| 删除规则 | 165 条 |
| 保留率 | 98.4% |

---

## 💡 为什么这样处理

### Q1: 为什么删除 DOMAIN-KEYWORD?

**A**: Xray 26.x 不兼容,无法绕过。删除 1.6% 的规则,保证 98.4% 的规则可用。

### Q2: 为什么要验证每个域名?

**A**: 一个非法域名会导致整个文件被拒绝。提前验证,确保 100% 可导入。

### Q3: 为什么要按策略分组?

**A**: 减少规则数量 (9,816 → 12),提高匹配效率,便于管理。

---

## 🚀 使用方法

### 1. 运行转换

```bash
python convert_clash_to_v2rayn.py
```

### 2. 导入 v2rayN

1. 清空旧规则
2. 导入 `v2rayn_routing_rules_new.json`
3. 配置 `block` 黑洞出站 (如果使用拦截功能)

---

## ⚠️ 注意事项

### 必须做的事

- ✅ 导入前清空旧规则
- ✅ 导入后测试常用网站
- ✅ 如使用拦截功能,配置黑洞出站

### 已删除的规则

- ❌ 所有 DOMAIN-KEYWORD (~100 条)
- ❌ 不符合 LDH 规范的域名 (~65 条)

### 可能受影响的功能

- OneDrive 关键词匹配
- Steam 关键词匹配
- 其他关键词规则

---

## 🔧 核心代码逻辑

### 域名验证

```python
def is_valid_domain_entry(entry: str) -> bool:
    val = entry.split(':', 1)[1]  # 提取域名
    
    if not val.isascii():  # 必须是 ASCII
        return False
    if '.' not in val:  # 必须含点
        return False
    
    for label in val.split('.'):
        if not label:  # 不能有空 label
            return False
        # LDH 规范: 字母/数字开头和结尾
        if not re.match(r'^[a-zA-Z0-9]([a-zA-Z0-9\-]*[a-zA-Z0-9])?$', label):
            return False
    
    return True
```

### 规则转换

```python
# DOMAIN → full:
if rule_type == 'DOMAIN':
    domain_entry = f'full:{rule_value}'
    if is_valid_domain_entry(domain_entry):
        domains.append(domain_entry)

# DOMAIN-SUFFIX → domain:
elif rule_type == 'DOMAIN-SUFFIX':
    domain_entry = f'domain:{rule_value}'
    if is_valid_domain_entry(domain_entry):
        domains.append(domain_entry)

# DOMAIN-KEYWORD → 删除
elif rule_type == 'DOMAIN-KEYWORD':
    continue  # 跳过

# IP-CIDR → 独立元素
elif rule_type in ['IP-CIDR', 'IP-CIDR6']:
    ip_value = rule_value.split(',')[0]  # 移除 no-resolve
    ips.append(ip_value)
```

### outboundTag 映射

```python
def convert_outbound_tag(policy: str) -> str:
    if policy == 'DIRECT':
        return 'direct'
    elif policy == 'REJECT':
        return 'block'
    else:
        return 'proxy'
```

---

## 📚 相关文档

| 文档 | 说明 |
|-----|------|
| 转换原理与设计文档.md | 详细的技术文档 |
| 快速参考.md | 一分钟导入指南 |
| 导入说明.md | 详细导入步骤 |
| 转换对比报告.md | 转换前后对比 |

---

## ✅ 总结

### 核心原理 (3 句话)

1. **读取分组**: 读取 Clash 配置,按策略分组
2. **转换验证**: 转换规则类型,验证域名格式
3. **输出 JSON**: 生成 v2rayN 可导入的 JSON 文件

### 为什么这样处理 (3 个原因)

1. **兼容性**: 遵循 Xray 26.x 的 LDH 规范
2. **可用性**: 删除不兼容规则,保证整体可用
3. **效率**: 按策略分组,减少规则数量

### 使用建议 (3 条)

1. **定期更新**: 每月重新转换一次
2. **验证结果**: 导入后测试常用网站
3. **手动补充**: 添加被删除的关键词规则

---

**版本**: v1.0  
**更新**: 2026-05-05  
**适用**: v2rayN v7.x · Xray 26.x
