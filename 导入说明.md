# v2rayN 路由规则导入说明

## 📋 转换结果

✅ **转换成功完成!**

- **原始规则数**: 9816 条
- **生成路由组**: 12 个
- **域名规则**: 9146 条
- **IP 规则**: 505 条
- **输出文件**: `v2rayn_routing_rules_new.json`

---

## 🚀 导入步骤

### 1. 清空现有规则（重要！）

在导入新规则前,**必须先清空 v2rayN 中的现有路由规则**,避免旧规则残留冲突。

**操作步骤**:
1. 打开 v2rayN
2. 点击菜单栏 `设置` → `路由设置`
3. 选中所有现有规则
4. 点击 `删除` 按钮
5. 点击 `确定` 保存

### 2. 导入新规则

**操作步骤**:
1. 在 v2rayN 中,点击菜单栏 `设置` → `路由设置`
2. 点击 `导入规则` 按钮
3. 选择文件 `v2rayn_routing_rules_new.json`
4. 点击 `确定` 完成导入

### 3. 配置黑洞出站（如果使用了拦截广告功能）

如果你的规则中使用了 `REJECT` 策略（如 `🛑 拦截广告`）,需要手动添加一个黑洞出站。

**操作步骤**:
1. 在 v2rayN 主界面,点击菜单栏 `设置` → `参数设置`
2. 切换到 `Core:路由设置` 标签
3. 在 `出站设置` 区域,点击 `添加`
4. 配置如下:
   - **Tag**: `block`
   - **Protocol**: `blackhole`
5. 点击 `确定` 保存

---

## ⚠️ 重要说明

### 已删除的规则类型

根据 Xray 26.x 的兼容性要求,以下规则类型已被删除:

1. **DOMAIN-KEYWORD 规则** - 全部删除
   - 原因: Xray 26.x 对 keyword 类型执行严格的 LDH 校验,导致大量规则报错
   - 影响: 约 100+ 条关键词匹配规则失效
   - 示例: `keyword:google`, `keyword:steam`, `keyword:spotify` 等

2. **GEOIP 规则** - 全部删除
   - 原因: 可能引发额外的兼容性问题
   - 建议: 如需地理位置路由,请在 v2rayN 中手动配置

### 域名过滤规则

转换过程中自动过滤了以下不合法的域名:

- ❌ 不含 `.` 的单级域名 (如 `domain:cn`, `domain:hk`)
- ❌ label 首尾有连字符的域名 (如 `domain:fc-.cdn.bcebos.com`)
- ❌ 含非 ASCII 字符的域名
- ❌ 空 label 或连续点的域名
- ❌ 占位符域名 (如 `domain:gfwlist.start`)

### outboundTag 对照表

| Clash 策略 | v2rayN outboundTag | 说明 |
|-----------|-------------------|------|
| `DIRECT` | `direct` | 直连 |
| `REJECT` | `block` | 拦截（需配置黑洞出站） |
| 其他代理策略 | `proxy` | 走代理 |

---

## 🔍 验证导入结果

导入完成后,建议进行以下验证:

1. **检查规则数量**
   - 在路由设置中应该看到 12 条路由规则
   - 每条规则的 `remarks` 字段显示对应的策略名称

2. **测试常用网站**
   - 测试国内网站是否直连
   - 测试需要代理的网站是否正常访问
   - 测试广告拦截是否生效

3. **查看日志**
   - 如果遇到问题,可以在 v2rayN 中查看日志
   - 菜单栏 `帮助` → `查看日志`

---

## 📝 已知限制

### 功能损失

删除 DOMAIN-KEYWORD 规则后,以下匹配功能失效:

- 含 `google` 关键词的未知子域名
- 含 `steam` 关键词的域名  
- 含 `spotify` 关键词的域名
- 含 `1drv`, `onedrive`, `skydrive` 关键词的 OneDrive 相关域名
- 其他约 100 条关键词规则

### 补救方案

如果某些网站无法正常访问,可以:

1. **手动添加具体域名**
   - 在 v2rayN 路由设置中手动添加 `full:` 或 `domain:` 规则
   - 不要使用 keyword 类型

2. **使用通配符域名**
   - 例如: `domain:google.com` 可以匹配所有 `*.google.com` 的子域名

---

## 🛠️ 故障排除

### 问题 1: 导入后 v2rayN 报错

**可能原因**: 旧规则残留冲突

**解决方案**:
1. 完全卸载 v2rayN
2. 删除配置文件夹 `%APPDATA%\v2rayN`
3. 重新安装 v2rayN
4. 重新导入规则

### 问题 2: 某些网站无法访问

**可能原因**: 该网站的规则使用了已删除的 DOMAIN-KEYWORD

**解决方案**:
1. 查看 v2rayN 日志,找到被拦截的域名
2. 手动添加该域名的 `domain:` 规则

### 问题 3: 广告拦截不生效

**可能原因**: 未配置黑洞出站

**解决方案**:
按照上面 "配置黑洞出站" 的步骤添加 `block` 出站

---

## 📚 参考文档

- [Clash 转 v2rayN 路由规则解决方案](./Clash转v2rayN路由规则_解决方案.md)
- [v2rayN 官方文档](https://github.com/2dust/v2rayN)
- [Xray 路由规则文档](https://xtls.github.io/config/routing.html)

---

## ✅ 转换脚本

如果需要重新转换或修改规则,可以使用 `convert_clash_to_v2rayn.py` 脚本:

```bash
python convert_clash_to_v2rayn.py
```

脚本会自动:
- 读取 `clash.yml`
- 应用所有过滤规则
- 生成 `v2rayn_routing_rules_new.json`

---

**转换时间**: 2026-05-05  
**适用版本**: v2rayN v7.x · Xray 26.x · Windows
