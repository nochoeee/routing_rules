# Clash → v2rayN 转换对比报告

## 📊 转换统计

### 整体数据

| 项目 | 数量 |
|-----|------|
| 原始 Clash 规则总数 | 9,816 条 |
| 生成 v2rayN 路由组 | 12 个 |
| 有效域名规则 | 9,146 条 |
| 有效 IP 规则 | 505 条 |
| 删除的规则 | 约 165 条 |

### 规则类型分布

| Clash 规则类型 | 处理方式 | 转换后格式 | 数量 |
|--------------|---------|-----------|------|
| `DOMAIN` | ✅ 转换 | `full:xxx` | ~3,200 条 |
| `DOMAIN-SUFFIX` | ✅ 转换 | `domain:xxx` | ~5,900 条 |
| `DOMAIN-KEYWORD` | ❌ 删除 | - | ~100 条 |
| `IP-CIDR` | ✅ 转换 | 独立数组元素 | ~480 条 |
| `IP-CIDR6` | ✅ 转换 | 独立数组元素 | ~25 条 |
| `DST-PORT` | ✅ 转换 | 逗号分隔 | ~12 条 |
| `GEOIP` | ❌ 删除 | - | ~0 条 |
| `MATCH` | ⚠️ 忽略 | - | ~1 条 |

---

## 🔄 转换规则详解

### 1. DOMAIN 规则转换

**Clash 格式**:
```yaml
- DOMAIN,google.com,🔰 选择节点
- DOMAIN,youtube.com,🔰 选择节点
```

**v2rayN 格式**:
```json
{
  "domain": [
    "full:google.com",
    "full:youtube.com"
  ],
  "outboundTag": "proxy"
}
```

**转换说明**:
- 每条 `DOMAIN` 规则转换为 `full:` 前缀
- 每条域名作为独立数组元素
- ✅ 完全兼容 Xray 26.x

---

### 2. DOMAIN-SUFFIX 规则转换

**Clash 格式**:
```yaml
- DOMAIN-SUFFIX,google.com,🔰 选择节点
- DOMAIN-SUFFIX,youtube.com,🔰 选择节点
```

**v2rayN 格式**:
```json
{
  "domain": [
    "domain:google.com",
    "domain:youtube.com"
  ],
  "outboundTag": "proxy"
}
```

**转换说明**:
- 每条 `DOMAIN-SUFFIX` 规则转换为 `domain:` 前缀
- 自动匹配所有子域名
- ✅ 完全兼容 Xray 26.x

---

### 3. DOMAIN-KEYWORD 规则（已删除）

**Clash 格式**:
```yaml
- DOMAIN-KEYWORD,google,🔰 选择节点
- DOMAIN-KEYWORD,steam,🔰 选择节点
- DOMAIN-KEYWORD,1drv,☁️ OneDrive
- DOMAIN-KEYWORD,onedrive,☁️ OneDrive
```

**v2rayN 格式**:
```
❌ 已删除 - Xray 26.x 不支持
```

**删除原因**:
- Xray 26.x 对 `keyword:` 类型执行严格的 LDH 校验
- 许多关键词含有首尾连字符或特殊字符
- 尝试转换为 `regexp:` 也无效

**影响的规则示例**:
```yaml
- DOMAIN-KEYWORD,steampipe,DIRECT
- DOMAIN-KEYWORD,steamcontent,DIRECT
- DOMAIN-KEYWORD,1drv,☁️ OneDrive
- DOMAIN-KEYWORD,onedrive,☁️ OneDrive
- DOMAIN-KEYWORD,skydrive,☁️ OneDrive
- DOMAIN-KEYWORD,abematv.akamaized.net,🔰 选择节点
- DOMAIN-KEYWORD,avoddashs,🔰 选择节点
- DOMAIN-KEYWORD,bbcfmt,🔰 选择节点
- DOMAIN-KEYWORD,uk-live,🔰 选择节点
- DOMAIN-KEYWORD,voddazn,🔰 选择节点
- DOMAIN-KEYWORD,jooxweb-api,🔰 选择节点
- DOMAIN-KEYWORD,japonx,🔰 选择节点
- DOMAIN-KEYWORD,japronx,🔰 选择节点
- DOMAIN-KEYWORD,nowtv100,🔰 选择节点
- DOMAIN-KEYWORD,rthklive,🔰 选择节点
- DOMAIN-KEYWORD,apiproxy-device-prod-nlb-,🔰 选择节点
- DOMAIN-KEYWORD,dualstack.apiproxy-,🔰 选择节点
- DOMAIN-KEYWORD,netflixdnstest,🔰 选择节点
- DOMAIN-KEYWORD,nivod,🔰 选择节点
- DOMAIN-KEYWORD,olevod,🔰 选择节点
- DOMAIN-KEYWORD,hbogoasia,🔰 选择节点
```

**补救建议**:
对于重要的关键词规则,可以手动添加具体域名:
```json
{
  "domain": [
    "domain:steampipe.akamaized.net",
    "domain:steamcontent.com",
    "domain:onedrive.com",
    "domain:onedrive.live.com"
  ]
}
```

---

### 4. IP-CIDR 规则转换

**Clash 格式**:
```yaml
- IP-CIDR,45.43.32.234/32,🌏 爱奇艺&哔哩哔哩,no-resolve
- IP-CIDR,103.151.150.0/23,🌏 爱奇艺&哔哩哔哩,no-resolve
```

**v2rayN 格式**:
```json
{
  "ip": [
    "45.43.32.234/32",
    "103.151.150.0/23"
  ],
  "outboundTag": "proxy"
}
```

**转换说明**:
- 每条 IP 规则作为独立数组元素
- 自动移除 `no-resolve` 参数
- ⚠️ **关键**: 绝对不能用 `\n` 拼接多个 IP

**错误示例** (会导致解析失败):
```json
{
  "ip": ["45.43.32.234/32\n103.151.150.0/23"]  // ❌ 错误!
}
```

---

### 5. DST-PORT 规则转换

**Clash 格式**:
```yaml
- DST-PORT,25,DIRECT
- DST-PORT,110,DIRECT
- DST-PORT,143,DIRECT
```

**v2rayN 格式**:
```json
{
  "port": "25,110,143",
  "outboundTag": "direct"
}
```

**转换说明**:
- 同一策略的多个端口合并为一条规则
- 端口用逗号分隔
- ✅ 完全兼容 Xray 26.x

---

## 🗂️ 策略组对照

### 生成的 12 个路由组

| 序号 | Clash 策略名 | v2rayN outboundTag | 域名数 | IP数 | 端口数 |
|-----|-------------|-------------------|-------|------|-------|
| 1 | 🌏 爱奇艺&哔哩哔哩 | proxy | 12 | 12 | 0 |
| 2 | 📺 动画疯 | proxy | 5 | 0 | 0 |
| 3 | 🔰 选择节点 | proxy | ~8,500 | ~470 | 0 |
| 4 | 🎮 Steam 登录/下载 | proxy | 2 | 0 | 0 |
| 5 | 🎮 Steam 商店/社区 | proxy | ~30 | 0 | 0 |
| 6 | ☁️ OneDrive | proxy | ~14 | 0 | 0 |
| 7 | 🎓学术网站 | proxy | ~17 | 0 | 0 |
| 8 | 🌩️ Cloudflare | proxy | 0 | 0 | 0 |
| 9 | 🇨🇳 国内网站 | direct | 0 | 0 | 0 |
| 10 | 🛑 拦截广告 | block | 0 | 0 | 0 |
| 11 | 🐟 漏网之鱼 | proxy | 0 | 0 | 0 |
| 12 | DIRECT | direct | ~550 | ~23 | 12 |

---

## ⚠️ 域名过滤详情

### 被过滤的非法域名类型

转换过程中自动过滤了以下不符合 LDH 规范的域名:

#### 1. 单级 TLD (不含 `.`)
```
❌ domain:cn
❌ domain:hk
❌ domain:eu
❌ domain:io
```

#### 2. Label 首尾有连字符
```
❌ domain:fc-.cdn.bcebos.com
❌ domain:-example.com
❌ domain:example-.com
```

#### 3. 占位符域名
```
❌ domain:gfwlist.start
❌ domain:gfwlist.end
```

#### 4. 空 Label
```
❌ domain:.example.com
❌ domain:example..com
```

#### 5. 非 ASCII 字符
```
❌ domain:中文域名.com
❌ domain:日本語.jp
```

### LDH 规范说明

每个域名 label 必须满足:
```regex
^[a-zA-Z0-9]([a-zA-Z0-9\-]*[a-zA-Z0-9])?$
```

即:
- ✅ 只能包含字母、数字、连字符
- ✅ 不能以连字符开头
- ✅ 不能以连字符结尾
- ✅ 必须包含至少一个 `.`

---

## 📈 转换效率

### 规则保留率

| 规则类型 | 原始数量 | 保留数量 | 保留率 |
|---------|---------|---------|-------|
| 域名规则 | ~9,200 | ~9,146 | 99.4% |
| IP 规则 | ~505 | ~505 | 100% |
| 端口规则 | ~12 | ~12 | 100% |
| 关键词规则 | ~100 | 0 | 0% |
| **总计** | **~9,817** | **~9,663** | **98.4%** |

### 文件大小对比

| 文件 | 大小 | 格式 |
|-----|------|------|
| clash.yml | ~850 KB | YAML |
| v2rayn_routing_rules_new.json | ~620 KB | JSON |

---

## ✅ 质量保证

### 验证通过的检查项

- ✅ 所有域名符合 LDH 规范
- ✅ IP 规则每条独立,无 `\n` 拼接
- ✅ 端口规则正确合并
- ✅ outboundTag 正确映射
- ✅ JSON 格式完全有效
- ✅ 无 DOMAIN-KEYWORD 残留
- ✅ 无 GEOIP 残留
- ✅ 无非法字符

### 兼容性确认

- ✅ v2rayN v7.x
- ✅ Xray-core 26.x
- ✅ Windows 10/11
- ✅ 支持 IPv4 和 IPv6

---

## 🔧 后续优化建议

### 1. 补充关键词规则

对于被删除的关键词规则,建议手动添加常用域名:

**OneDrive 相关**:
```json
{
  "domain": [
    "domain:onedrive.com",
    "domain:onedrive.live.com",
    "domain:sharepoint.com",
    "domain:1drv.com"
  ],
  "outboundTag": "proxy"
}
```

**Steam 相关**:
```json
{
  "domain": [
    "domain:steampipe.akamaized.net",
    "domain:steamcontent.com",
    "domain:steamcdn.com"
  ],
  "outboundTag": "direct"
}
```

### 2. 定期更新规则

建议每月重新转换一次,以获取最新的规则:

```bash
# 1. 更新 clash.yml
# 2. 运行转换脚本
python convert_clash_to_v2rayn.py

# 3. 重新导入到 v2rayN
```

### 3. 监控日志

导入后的前几天,建议:
- 定期查看 v2rayN 日志
- 记录无法访问的网站
- 手动添加缺失的域名规则

---

**报告生成时间**: 2026-05-05  
**转换工具版本**: 1.0  
**适用环境**: v2rayN v7.x · Xray 26.x · Windows
