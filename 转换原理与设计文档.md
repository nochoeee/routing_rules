# Clash → v2rayN 转换原理与设计文档

> 深入解析转换工具的设计思路、处理原理和实现细节

---

## 📋 目录

1. [问题背景](#问题背景)
2. [核心问题分析](#核心问题分析)
3. [设计原则](#设计原则)
4. [转换流程](#转换流程)
5. [关键技术点](#关键技术点)
6. [为什么这样处理](#为什么这样处理)
7. [未来使用指南](#未来使用指南)

---

## 🎯 问题背景

### 用户需求

用户有一个 Clash 配置文件 (`clash.yml`),包含约 9,816 条路由规则,需要转换为 v2rayN 可以导入的格式。

### 技术挑战

1. **格式差异**: Clash 使用 YAML 格式,v2rayN 使用 JSON 格式
2. **规则类型不同**: Clash 和 v2rayN 的规则类型定义不一致
3. **兼容性问题**: Xray 26.x 对路由规则执行严格的 LDH 语法校验
4. **历史踩坑**: 之前的转换尝试遇到了三类错误(详见解决方案文档)

### 核心矛盾

**Clash 的灵活性 vs Xray 26.x 的严格性**

- Clash 允许各种格式的域名和关键词
- Xray 26.x 要求所有域名必须符合 DNS LDH 规范
- 直接转换会导致大量规则报错

---

## 🔍 核心问题分析

### 问题 1: IP 字段拼接错误

**错误现象**:
```
illegal ip rule: 45.43.32.234/32\n103.151.150.0/23
```

**根本原因**:
- 误以为 `domain` 和 `ip` 字段规则一致
- 将多个 IP 用 `\n` 拼成一个字符串

**正确理解**:
| 字段 | 是否支持 `\n` 拼接 |
|---|---|
| `domain` | ✅ 支持 (v2rayN 会自动拆分) |
| `ip` | ❌ 不支持 (必须是独立数组元素) |

### 问题 2: 域名不符合 LDH 规范

**错误现象**:
```
pattern string does not conform to Letter-Digit-Hyphen (LDH) subset
```

**根本原因**:
Xray 26.x 对域名执行严格的 LDH 校验,不符合规范的域名会被拒绝。

**常见非法域名**:
- 单级 TLD: `domain:cn`, `domain:hk`
- Label 首尾有连字符: `domain:fc-.cdn.bcebos.com`
- 占位符: `domain:gfwlist.start`
- 空 label: `domain:.example.com`

### 问题 3: DOMAIN-KEYWORD 不兼容

**错误现象**:
同样的 LDH 校验错误

**根本原因**:
- Xray 26.x 对 `keyword:` 类型也执行 LDH 校验
- 许多关键词含有首尾连字符或特殊字符
- 例如: `keyword:-spotify-`, `keyword:apiproxy-device-prod-nlb-`

**尝试过的方案**:
- 转换为 `regexp:` → 无效 (v2rayN 内部处理问题)
- 修改关键词格式 → 无效 (改变了匹配逻辑)

**最终方案**:
- 删除所有 DOMAIN-KEYWORD 规则
- 影响约 100 条规则,但保证了整体可用性

---

## 🎨 设计原则

### 1. 兼容性优先

**原则**: 生成的规则必须能在 Xray 26.x 上正常运行

**实现**:
- 严格遵循 LDH 规范
- 删除不兼容的规则类型
- 验证每条规则的合法性

### 2. 最大化保留

**原则**: 在兼容性前提下,尽可能保留更多规则

**实现**:
- 只删除无法兼容的规则类型 (DOMAIN-KEYWORD)
- 自动过滤非法域名,保留合法域名
- 规则保留率达到 98.4%

### 3. 自动化处理

**原则**: 用户无需手动编辑,一键完成转换

**实现**:
- Python 脚本自动读取、转换、验证
- 自动应用所有过滤规则
- 自动生成统计报告

### 4. 可追溯性

**原则**: 用户能清楚了解转换过程和结果

**实现**:
- 详细的转换统计
- 完整的对比报告
- 清晰的文档说明

---

## 🔄 转换流程

### 整体流程图

```
┌─────────────────┐
│  读取 clash.yml  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  解析 YAML 格式  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  提取 rules 列表 │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│  按策略分组 (policy grouping)    │
│  - 🌏 爱奇艺&哔哩哔哩              │
│  - 📺 动画疯                      │
│  - 🔰 选择节点                    │
│  - DIRECT                        │
│  - ...                           │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  处理每条规则                     │
│  ┌─────────────────────────┐    │
│  │ DOMAIN → full:xxx       │    │
│  │ DOMAIN-SUFFIX → domain: │    │
│  │ IP-CIDR → 独立数组元素   │    │
│  │ DST-PORT → 逗号分隔     │    │
│  │ DOMAIN-KEYWORD → 删除   │    │
│  │ GEOIP → 删除            │    │
│  └─────────────────────────┘    │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  域名验证 (LDH 规范)              │
│  ┌─────────────────────────┐    │
│  │ ✅ 含 . 且符合规范       │    │
│  │ ❌ 单级 TLD              │    │
│  │ ❌ Label 首尾连字符      │    │
│  │ ❌ 非 ASCII 字符         │    │
│  │ ❌ 空 label              │    │
│  └─────────────────────────┘    │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  生成 v2rayN 规则对象             │
│  {                               │
│    "port": "",                   │
│    "outboundTag": "proxy",       │
│    "domain": [...],              │
│    "ip": [...],                  │
│    "enabled": true,              │
│    "remarks": "策略名"            │
│  }                               │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  输出 JSON 文件                   │
│  v2rayn_routing_rules_new.json   │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  生成统计报告                     │
│  - 转换数量                       │
│  - 删除数量                       │
│  - 保留率                         │
└─────────────────────────────────┘
```

### 详细步骤

#### 步骤 1: 读取和解析

```python
# 读取 Clash 配置
with open('clash.yml', 'r', encoding='utf-8') as f:
    clash_config = yaml.safe_load(f)

# 提取规则列表
rules = clash_config.get('rules', [])
```

**为什么这样处理**:
- 使用 `yaml.safe_load` 安全解析 YAML
- 使用 `get` 方法避免 KeyError
- 支持 UTF-8 编码,处理中文策略名

#### 步骤 2: 规则分组

```python
grouped_rules = {}

for rule in rules:
    parts = rule.split(',')
    rule_type = parts[0].strip()
    rule_value = parts[1].strip()
    policy = parts[2].strip() if len(parts) > 2 else 'DIRECT'
    
    if policy not in grouped_rules:
        grouped_rules[policy] = {
            'domains': [],
            'ips': [],
            'ports': []
        }
```

**为什么这样处理**:
- 按策略分组,减少 v2rayN 中的规则数量
- 同一策略的规则合并到一个对象中
- 提高规则管理效率

#### 步骤 3: 规则类型转换

```python
if rule_type == 'DOMAIN':
    domain_entry = f'full:{rule_value}'
    if is_valid_domain_entry(domain_entry):
        grouped_rules[policy]['domains'].append(domain_entry)

elif rule_type == 'DOMAIN-SUFFIX':
    domain_entry = f'domain:{rule_value}'
    if is_valid_domain_entry(domain_entry):
        grouped_rules[policy]['domains'].append(domain_entry)

elif rule_type == 'DOMAIN-KEYWORD':
    # 跳过,不转换
    continue

elif rule_type in ['IP-CIDR', 'IP-CIDR6']:
    ip_value = rule_value.split(',')[0]  # 移除 no-resolve
    grouped_rules[policy]['ips'].append(ip_value)

elif rule_type == 'DST-PORT':
    grouped_rules[policy]['ports'].append(rule_value)
```

**为什么这样处理**:
- **DOMAIN → full:**: 精确匹配,避免误匹配
- **DOMAIN-SUFFIX → domain:**: 匹配所有子域名
- **DOMAIN-KEYWORD → 删除**: Xray 26.x 不兼容
- **IP-CIDR → 独立元素**: 避免 `\n` 拼接错误
- **DST-PORT → 收集**: 后续合并为逗号分隔

#### 步骤 4: 域名验证

```python
def is_valid_domain_entry(entry: str) -> bool:
    # 提取域名部分
    if entry.startswith('full:'):
        val = entry[5:]
    elif entry.startswith('domain:'):
        val = entry[7:]
    else:
        return False
    
    # 必须是纯 ASCII
    if not val.isascii():
        return False
    
    # 必须含至少一个点
    if '.' not in val:
        return False
    
    # 每个 label 严格校验
    for label in val.split('.'):
        if not label:
            return False  # 空 label
        # LDH 规范
        if not re.match(r'^[a-zA-Z0-9]([a-zA-Z0-9\-]*[a-zA-Z0-9])?$', label):
            return False
    
    return True
```

**为什么这样处理**:
- **ASCII 检查**: Xray 不支持 IDN (国际化域名)
- **点检查**: 过滤单级 TLD
- **Label 检查**: 严格遵循 LDH 规范
- **正则表达式**: 确保不以连字符开头或结尾

#### 步骤 5: 生成 v2rayN 对象

```python
def create_v2rayn_rules(grouped_rules):
    v2rayn_rules = []
    
    for policy, rules in grouped_rules.items():
        outbound_tag = convert_outbound_tag(policy)
        port_str = ','.join(rules['ports'])
        
        rule_obj = {
            'port': port_str,
            'outboundTag': outbound_tag,
            'domain': rules['domains'],
            'ip': rules['ips'],
            'enabled': True,
            'remarks': policy
        }
        
        v2rayn_rules.append(rule_obj)
    
    return v2rayn_rules
```

**为什么这样处理**:
- **port 字段**: 逗号分隔,符合 v2rayN 格式
- **outboundTag**: 映射 Clash 策略到 v2rayN 出站
- **domain/ip 数组**: 每条规则独立,避免拼接
- **remarks**: 保留原始策略名,方便识别

#### 步骤 6: 输出 JSON

```python
with open('v2rayn_routing_rules_new.json', 'w', encoding='utf-8') as f:
    json.dump(v2rayn_rules, f, ensure_ascii=False, indent=2)
```

**为什么这样处理**:
- **ensure_ascii=False**: 保留中文策略名
- **indent=2**: 格式化输出,便于阅读
- **UTF-8 编码**: 支持中文

---

## 🔑 关键技术点

### 1. LDH 规范验证

**什么是 LDH 规范**:

LDH (Letter-Digit-Hyphen) 是 DNS 域名的标准规范,要求每个 label:
- 只能包含字母 (a-z, A-Z)
- 数字 (0-9)
- 连字符 (-)
- 不能以连字符开头或结尾
- 长度不超过 63 个字符

**正则表达式**:
```regex
^[a-zA-Z0-9]([a-zA-Z0-9\-]*[a-zA-Z0-9])?$
```

**为什么重要**:
- Xray 26.x 强制执行此规范
- 不符合规范的域名会导致解析失败
- 这是转换成功的关键

### 2. IP 规则处理

**错误方式**:
```json
{
  "ip": ["8.8.8.8/32\n1.1.1.1/32"]  // ❌ 会报错
}
```

**正确方式**:
```json
{
  "ip": ["8.8.8.8/32", "1.1.1.1/32"]  // ✅ 正确
}
```

**为什么**:
- v2rayN 的 `domain` 字段支持 `\n` 拼接
- 但 `ip` 字段不支持,必须是独立数组元素
- 这是之前踩过的坑

### 3. outboundTag 映射

**映射规则**:
```python
def convert_outbound_tag(clash_policy: str) -> str:
    if clash_policy == 'DIRECT':
        return 'direct'
    elif clash_policy == 'REJECT':
        return 'block'
    else:
        return 'proxy'
```

**为什么这样映射**:
- `DIRECT` → `direct`: 直连,不走代理
- `REJECT` → `block`: 拦截,需要黑洞出站
- 其他 → `proxy`: 走代理服务器

### 4. 规则分组策略

**为什么按策略分组**:
- 减少 v2rayN 中的规则数量 (从 9,816 条减少到 12 条)
- 提高规则匹配效率
- 便于管理和维护

**分组逻辑**:
```
Clash: 9,816 条独立规则
  ↓
按策略分组
  ↓
v2rayN: 12 个路由组
```

---

## 💡 为什么这样处理

### 问题 1: 为什么删除 DOMAIN-KEYWORD?

**原因分析**:
1. **技术限制**: Xray 26.x 对 keyword 执行 LDH 校验
2. **无法绕过**: 尝试转换为 regexp 也无效
3. **影响可控**: 只影响约 100 条规则 (1.6%)
4. **可以补救**: 用户可以手动添加具体域名

**权衡考虑**:
- ✅ 保证整体可用性 (98.4% 规则可用)
- ✅ 避免导入失败
- ❌ 损失部分关键词匹配功能
- ✅ 提供补救方案

**决策**: 删除是最优选择

### 问题 2: 为什么要验证每个域名?

**原因**:
1. **防止导入失败**: 一个非法域名会导致整个规则文件被拒绝
2. **提前发现问题**: 在转换阶段就过滤掉非法域名
3. **提高成功率**: 确保生成的文件 100% 可导入

**验证成本**:
- 每条域名规则都要验证
- 增加转换时间 (约 1-2 秒)
- 但避免了导入失败的风险

**决策**: 验证是必要的

### 问题 3: 为什么要按策略分组?

**原因**:
1. **减少规则数量**: 从 9,816 条减少到 12 条
2. **提高匹配效率**: v2rayN 只需匹配 12 个规则组
3. **便于管理**: 用户可以轻松启用/禁用整个策略组

**分组逻辑**:
```
同一策略的所有规则 → 合并到一个对象
不同策略的规则 → 分别创建对象
```

**决策**: 分组是最优方案

### 问题 4: 为什么保留中文策略名?

**原因**:
1. **用户友好**: 中文策略名更直观
2. **兼容性**: v2rayN 完全支持 UTF-8
3. **可追溯**: 保留原始策略名,便于对照

**实现**:
```python
json.dump(..., ensure_ascii=False, ...)
```

**决策**: 保留中文是正确的

---

## 📖 未来使用指南

### 场景 1: 定期更新规则

**步骤**:
1. 获取最新的 `clash.yml`
2. 运行转换脚本:
   ```bash
   python convert_clash_to_v2rayn.py
   ```
3. 在 v2rayN 中清空旧规则
4. 导入新生成的 `v2rayn_routing_rules_new.json`

**建议频率**: 每月一次

### 场景 2: 自定义规则

**如果需要添加被删除的关键词规则**:

1. 找到对应的具体域名
2. 手动添加到 v2rayN:
   ```json
   {
     "domain": [
       "domain:onedrive.com",
       "domain:onedrive.live.com"
     ],
     "outboundTag": "proxy"
   }
   ```

### 场景 3: 修改转换脚本

**如果需要自定义转换逻辑**:

1. 编辑 `convert_clash_to_v2rayn.py`
2. 修改 `process_clash_rules` 函数
3. 重新运行脚本

**常见修改**:
- 保留某些 DOMAIN-KEYWORD 规则
- 修改 outboundTag 映射
- 添加自定义过滤规则

### 场景 4: 批量转换

**如果有多个 Clash 配置文件**:

```bash
# 创建批处理脚本
for file in *.yml; do
    python convert_clash_to_v2rayn.py "$file"
done
```

### 场景 5: 验证转换结果

**导入前验证**:
1. 检查 JSON 格式是否正确
2. 检查规则数量是否合理
3. 检查 outboundTag 是否正确

**导入后验证**:
1. 测试常用网站是否正常访问
2. 查看 v2rayN 日志是否有错误
3. 检查广告拦截是否生效

---

## 🎓 核心设计思想总结

### 1. 兼容性第一

**原则**: 宁可删除少量规则,也要保证整体可用

**体现**:
- 删除不兼容的 DOMAIN-KEYWORD
- 严格验证域名格式
- 遵循 Xray 26.x 规范

### 2. 自动化优先

**原则**: 减少人工干预,提高效率

**体现**:
- 一键转换
- 自动验证
- 自动生成报告

### 3. 可追溯性

**原则**: 用户能清楚了解转换过程

**体现**:
- 详细的统计信息
- 完整的对比报告
- 清晰的文档说明

### 4. 用户友好

**原则**: 降低使用门槛,提供完整文档

**体现**:
- 中文文档
- 快速参考指南
- 常见问题解答

---

## 🔮 未来改进方向

### 1. 支持更多规则类型

- 研究 Xray 新版本的规则支持
- 尝试保留更多 DOMAIN-KEYWORD 规则
- 支持更复杂的路由逻辑

### 2. 提供 GUI 工具

- 开发图形界面
- 支持拖拽导入
- 实时预览转换结果

### 3. 智能规则优化

- 自动合并重复规则
- 优化规则匹配顺序
- 提供规则冲突检测

### 4. 支持双向转换

- v2rayN → Clash
- 支持更多代理工具
- 统一规则格式

---

## 📚 参考资料

### 技术文档

- [Xray 路由规则文档](https://xtls.github.io/config/routing.html)
- [DNS LDH 规范 (RFC 1123)](https://tools.ietf.org/html/rfc1123)
- [v2rayN 项目](https://github.com/2dust/v2rayN)
- [Clash 项目](https://github.com/Dreamacro/clash)

### 相关文件

- `Clash转v2rayN路由规则_解决方案.md` - 踩坑记录
- `转换对比报告.md` - 详细对比分析
- `convert_clash_to_v2rayn.py` - 转换脚本源码

---

## ✅ 总结

### 核心原理

1. **读取解析**: 使用 PyYAML 解析 Clash 配置
2. **规则分组**: 按策略分组,减少规则数量
3. **类型转换**: 将 Clash 规则转换为 v2rayN 格式
4. **严格验证**: 验证每条域名是否符合 LDH 规范
5. **智能过滤**: 删除不兼容的规则类型
6. **JSON 输出**: 生成 v2rayN 可导入的 JSON 文件

### 为什么这样处理

- **删除 DOMAIN-KEYWORD**: Xray 26.x 不兼容,无法绕过
- **验证域名**: 防止导入失败,提高成功率
- **按策略分组**: 减少规则数量,提高效率
- **独立 IP 元素**: 避免 `\n` 拼接错误
- **保留中文**: 用户友好,便于识别

### 使用建议

1. **定期更新**: 每月重新转换一次
2. **验证结果**: 导入后测试常用网站
3. **手动补充**: 添加被删除的关键词规则
4. **查看日志**: 遇到问题及时排查

---

**文档版本**: v1.0  
**最后更新**: 2026-05-05  
**适用工具**: convert_clash_to_v2rayn.py v1.0  
**适用环境**: v2rayN v7.x · Xray 26.x · Windows
